A_JumpIfCloser
==============

.. action:: A_JumpIfCloser

	Jumps to the specified state if the calling actor's target is closer than the specified distance in units (pixels).

	:param distance: Distance from target after which the jump will not occur.
	:param frame: State to jump to if distance check passes.
