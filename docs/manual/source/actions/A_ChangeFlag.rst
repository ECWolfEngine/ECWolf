A_ChangeFlag
============

.. action:: A_ChangeFlag

	Either sets or unsets a flag based on value. The flag can be any of the :ref:`actor flags <Actor flags>`, but it may not always produce a useful result.

	:param flag: Flag to change.
	:param value: Whether to set or unset the flag.
